# 爬虫定时任务配置说明

## 📋 概述

本说明介绍如何配置Windows定时任务，让爬虫程序每天自动运行，爬取前一天的竞品社媒数据。

## 🚀 方法一：使用Windows任务计划程序（推荐）

### 步骤1：准备脚本文件

项目中已经提供了两个脚本文件：
- `run_daily_scraper.bat` - 批处理脚本（简单版本）
- `run_daily_scraper.ps1` - PowerShell脚本（带日志功能）

**建议使用PowerShell脚本**，因为它会自动保存日志文件。

### 步骤2：创建Windows定时任务

#### 2.1 打开任务计划程序

1. 按 `Win + R` 打开运行对话框
2. 输入 `taskschd.msc` 并回车
3. 或者：开始菜单 → Windows管理工具 → 任务计划程序

#### 2.2 创建基本任务

1. 在右侧点击 **"创建基本任务"**
2. 输入任务名称，例如：`竞品数据每日爬取`
3. 输入任务描述（可选）：`每天自动爬取前一天的所有竞品社媒数据`
4. 点击 **"下一步"**

#### 2.3 设置触发器

1. 选择 **"每天"**
2. 点击 **"下一步"**
3. 设置开始时间，例如：**每天 09:00**（建议设置在非工作时间）
4. 设置重复间隔：**1天**
5. 点击 **"下一步"**

#### 2.4 设置操作

**如果使用批处理脚本 (.bat)：**
1. 选择 **"启动程序"**
2. 点击 **"下一步"**
3. 程序或脚本：浏览选择 `run_daily_scraper.bat`
4. 起始于（可选）：输入脚本所在目录的完整路径，例如：`D:\GURU\-`
5. 点击 **"下一步"**

**如果使用PowerShell脚本 (.ps1)：**
1. 选择 **"启动程序"**
2. 点击 **"下一步"**
3. 程序或脚本：输入 `powershell.exe`
4. 添加参数（可选参数）：输入以下内容（替换为实际路径）
   ```
   -ExecutionPolicy Bypass -File "D:\GURU\-\run_daily_scraper.ps1"
   ```
5. 起始于（可选）：输入脚本所在目录，例如：`D:\GURU\-`
6. 点击 **"下一步"**

#### 2.5 完成配置

1. 勾选 **"当单击完成时，打开此任务属性的对话框"**
2. 点击 **"完成"**

#### 2.6 高级设置（重要）

在任务属性对话框中：

1. **常规** 标签页：
   - 勾选 **"不管用户是否登录都要运行"**
   - 勾选 **"使用最高权限运行"**（如果需要）
   - 配置为：**"只在以下Windows版本上运行"** → 选择你的Windows版本

2. **触发器** 标签页（重要！）：
   - 双击你的触发器（例如"每天 09:00"）
   - 在触发器编辑对话框中，点击 **"高级设置"**
   - **关键设置**：勾选 **"如果错过了计划的开始时间，立即启动任务"**
   - 这样即使电脑在9点未开机，开机后也会立即执行任务
   - 点击 **"确定"** 保存触发器设置

3. **条件** 标签页：
   - 取消勾选 **"只有在计算机使用交流电源时才启动此任务"**（如果使用笔记本）
   - 取消勾选 **"只有在以下网络连接可用时才启动"**（如果需要）

4. **设置** 标签页：
   - 勾选 **"允许按需运行任务"**
   - 勾选 **"如果任务失败，重新启动"**，设置间隔：5分钟，尝试重新启动最多：3次
   - **"如果请求的任务正在运行，则以下规则适用"**：建议选择 **"如果现有任务实例正在运行，则停止现有实例"**（避免任务重复执行）

5. 点击 **"确定"** 保存

### 步骤3：测试任务

1. 在任务计划程序中找到刚创建的任务
2. 右键点击任务 → **"运行"**
3. 检查是否正常执行
4. 查看日志文件（如果使用PowerShell脚本）：`logs/daily_scraper_YYYY-MM-DD.log`

## 🛠️ 方法二：使用Python定时脚本

### 创建Python定时脚本

创建一个 `daily_scheduler.py` 文件：

```python
import schedule
import time
import subprocess
import sys
from datetime import datetime

def run_scraper():
    """运行爬虫程序"""
    print(f"[{datetime.now()}] 开始执行每日爬虫任务...")
    
    # 运行爬虫程序，爬取昨天的数据
    result = subprocess.run(
        [sys.executable, "CompetitorDailyScraperFromDB.py", "--days-ago", "1"],
        cwd=".",  # 脚本所在目录
        capture_output=True,
        text=True
    )
    
    if result.returncode == 0:
        print(f"[{datetime.now()}] 爬虫任务执行成功")
        print(result.stdout)
    else:
        print(f"[{datetime.now()}] 爬虫任务执行失败")
        print(result.stderr)
    
    return result.returncode

# 每天 09:00 执行
schedule.every().day.at("09:00").do(run_scraper)

# 也可以使用其他时间格式
# schedule.every().day.at("09:30").do(run_scraper)  # 每天 09:30
# schedule.every(1).days.at("02:00").do(run_scraper)  # 每天 02:00

print("定时任务已启动，将在每天 09:00 执行爬虫任务...")
print("按 Ctrl+C 停止")

# 主循环
try:
    while True:
        schedule.run_pending()
        time.sleep(60)  # 每分钟检查一次
except KeyboardInterrupt:
    print("\n定时任务已停止")
```

### 运行Python定时脚本

```powershell
# 安装依赖
pip install schedule

# 运行定时脚本（需要在后台运行）
python daily_scheduler.py
```

**注意**：这种方法需要保持脚本持续运行，适合在服务器或长时间运行的机器上使用。

## 📝 方法三：使用第三方任务调度工具（高级）

### 使用 APScheduler

创建 `scheduler.py`：

```python
from apscheduler.schedulers.blocking import BlockingScheduler
from apscheduler.triggers.cron import CronTrigger
import subprocess
import sys
from datetime import datetime

def run_scraper():
    """运行爬虫程序"""
    print(f"[{datetime.now()}] 开始执行每日爬虫任务...")
    
    result = subprocess.run(
        [sys.executable, "CompetitorDailyScraperFromDB.py", "--days-ago", "1"],
        cwd=".",
        capture_output=True,
        text=True
    )
    
    if result.returncode == 0:
        print(f"[{datetime.now()}] 爬虫任务执行成功")
    else:
        print(f"[{datetime.now()}] 爬虫任务执行失败")
        print(result.stderr)
    
    return result.returncode

# 创建调度器
scheduler = BlockingScheduler()

# 每天 09:00 执行
scheduler.add_job(
    run_scraper,
    trigger=CronTrigger(hour=9, minute=0),
    id='daily_scraper',
    name='每日爬虫任务'
)

print("定时任务已启动，将在每天 09:00 执行爬虫任务...")
print("按 Ctrl+C 停止")

try:
    scheduler.start()
except KeyboardInterrupt:
    print("\n定时任务已停止")
    scheduler.shutdown()
```

安装和运行：

```powershell
pip install apscheduler
python scheduler.py
```

## ✅ 验证配置

### 检查任务是否配置成功

1. 在任务计划程序中查看任务状态
2. 手动运行一次任务测试
3. 检查数据库是否有新数据
4. 查看日志文件（如果使用PowerShell脚本）

### 查看执行历史

1. 在任务计划程序中找到任务
2. 点击 **"历史记录"** 标签页
3. 查看任务的执行记录

## 🔧 常见问题

### 问题1：任务没有执行（特别是电脑未开机时）

**可能原因：**
- 任务被禁用
- 触发器设置错误
- 未启用"如果错过了计划的开始时间，立即启动任务"
- 用户权限不足

**解决方法：**
- 检查任务是否启用（右键任务 → 启用）
- **重要**：在触发器的高级设置中，勾选 **"如果错过了计划的开始时间，立即启动任务"**
  - 这样即使电脑在设定时间未开机，开机后也会立即执行错过的任务
- 检查触发器设置
- 确保使用了正确的用户账户和权限

### 问题1.1：电脑未开机时错过了任务

**解决方案：**
如果设置了 **"如果错过了计划的开始时间，立即启动任务"**，当你开机后：
- 系统会检测到错过了执行时间
- 会在你登录后（或开机后）立即执行任务
- 确保不会因为电脑未开机而丢失数据爬取

**注意**：
- 这个设置确保即使偶尔晚开机，也能及时爬取数据
- 建议在触发器的"高级设置"中启用此选项

### 问题2：脚本找不到Python

**解决方法：**
- 在脚本中使用完整路径，例如：`C:\Python39\python.exe`
- 或者将Python添加到系统PATH环境变量

### 问题3：虚拟环境激活失败

**解决方法：**
- 在脚本中修改虚拟环境的路径
- 或者直接使用完整路径调用虚拟环境中的Python，例如：
  ```batch
  .venv\Scripts\python.exe CompetitorDailyScraperFromDB.py --days-ago 1
  ```

## 📚 推荐配置

**最佳实践：**

1. **执行时间**：建议设置在 **每天上午 9:00** 或 **每天凌晨 2:00**（避开工作时间）
2. **脚本选择**：推荐使用 **PowerShell脚本**（`run_daily_scraper.ps1`），因为有日志功能
3. **监控方式**：定期检查日志文件，确保任务正常执行
4. **备份策略**：定期备份数据库文件

## 📞 获取帮助

如果遇到问题，可以：
1. 查看日志文件：`logs/daily_scraper_YYYY-MM-DD.log`
2. 手动运行脚本测试：`python CompetitorDailyScraperFromDB.py --days-ago 1`
3. 检查任务计划程序中的错误信息
