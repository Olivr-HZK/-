# 每日爬虫（scrapers/daily_scraper）与每周周报工作流（workflows/period_workflow）共用

# HTTP 请求（RapidAPI、飞书/企微 webhook 等）
requests>=2.32.5,<3.0.0

# YAML（rapidapi 配置、飞书卡片等）
PyYAML>=6.0.3,<7.0.0

# 环境变量（env_loader、period_workflow）
python-dotenv>=1.0.0

# 周报 AI 分析（analyzers/period_ai、daily_ai，兼容 OpenRouter）
openai>=1.0.0
