# 竞品数据爬取使用说明

## 📋 概述

`CompetitorDailyScraperFromDB.py` 是一个从数据库读取竞品公司社媒配置，自动爬取各平台（Twitter、TikTok、Instagram、YouTube、Facebook）的更新数据并保存到数据库的程序。

## 🚀 快速开始

### 基础用法

#### 1. 爬取所有公司今天的数据

```powershell
python CompetitorDailyScraperFromDB.py
```

#### 2. 爬取所有公司昨天的数据

```powershell
python CompetitorDailyScraperFromDB.py --days-ago 1
```

#### 3. 爬取指定公司的数据

```powershell
# 爬取单个公司
python CompetitorDailyScraperFromDB.py --companies "dream games"

# 爬取多个公司（用空格分隔）
python CompetitorDailyScraperFromDB.py --companies "dream games" "vita studio"
```

#### 4. 爬取指定公司的昨天数据

```powershell
python CompetitorDailyScraperFromDB.py --companies "dream games" "vita studio" --days-ago 1
```

#### 5. 爬取指定日期的数据

```powershell
python CompetitorDailyScraperFromDB.py --date 2026-01-15 --companies "dream games"
```

## 📖 命令行参数说明

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `--date` | YYYY-MM-DD | 无 | 指定目标日期（如果指定，则忽略 `--days-ago`） |
| `--days-ago` | 整数 | 0 | 爬取多少天前的数据（0=今天，1=昨天，2=前天...） |
| `--companies` | 字符串列表 | 所有公司 | 指定要爬取的公司列表（空格分隔） |
| `--db-path` | 文件路径 | `db/competitor_data.db` | 数据库文件路径（可选） |

## 💡 使用示例

### 示例1：只爬取 dream games 和 vita studio 今天的数据

```powershell
python CompetitorDailyScraperFromDB.py --companies "dream games" "vita studio"
```

### 示例2：爬取 dream games 和 vita studio 昨天所有平台的数据

```powershell
python CompetitorDailyScraperFromDB.py --companies "dream games" "vita studio" --days-ago 1
```

### 示例3：补爬指定日期的数据

```powershell
# 补爬 2026-01-10 的数据
python CompetitorDailyScraperFromDB.py --date 2026-01-10 --companies "dream games" "vita studio"
```

### 示例4：爬取所有公司的数据（默认行为）

```powershell
python CompetitorDailyScraperFromDB.py --days-ago 1
```

## 🔍 支持的平台

程序会自动从数据库读取每个公司的平台配置，支持以下平台：

- **Twitter/X** - 爬取推文
- **TikTok** - 爬取视频
- **Instagram** - 爬取帖子
- **YouTube** - 爬取视频和 Shorts
- **Facebook** - 爬取帖子

## 📊 输出说明

### 数据存储位置

爬取的数据会保存到数据库 `db/competitor_data.db` 中，每个公司的数据存储在对应的表中。

### 控制台输出

程序运行时会显示：

```
============================================================
🕷️ 从数据库读取配置，爬取竞品社媒数据
============================================================
📅 目标日期: 2026-01-15
📋 找到 2 个公司

============================================================
🏢 处理公司: dream games
============================================================
  📋 找到 4 个启用的平台
    [Twitter] dream games
      URL: https://x.com/dreamgames
      ✓ 获取到 12 条推文
    [Instagram] dream games
      URL: https://www.instagram.com/dreamgames/
      ✓ 获取到 5 条帖子
    ...

  💾 保存数据到数据库...
  ✓ dream games 数据保存成功（4 个平台，25 条帖子）

============================================================
📊 爬取总结
============================================================
  处理公司数: 2
  处理平台数: 8
  总帖子数: 45
  成功: 2
  失败: 0
```

## ⚙️ 配置要求

### 1. 环境变量配置

确保在 `.env` 文件中配置了 RapidAPI Key：

```env
RAPIDAPI_KEY=your_api_key_here
# 或配置多个 API Key（用分号分隔）
RAPIDAPI_KEY=key1;key2;key3
```

### 2. 数据库配置

确保数据库 `db/competitor_data.db` 中已经配置了公司信息和平台信息。

## 🔧 高级用法

### 使用自定义数据库路径

```powershell
python CompetitorDailyScraperFromDB.py --db-path /path/to/custom.db --companies "dream games"
```

### 爬取多个公司的不同日期

```powershell
# 分别爬取不同日期
python CompetitorDailyScraperFromDB.py --date 2026-01-14 --companies "dream games"
python CompetitorDailyScraperFromDB.py --date 2026-01-15 --companies "vita studio"
```

## ⚠️ 注意事项

1. **API 限流**：程序已经实现了 API key 轮换和请求延迟机制，但如果遇到 429 错误，请：
   - 检查 API key 是否有效
   - 配置多个 API key 以自动切换
   - 等待一段时间后重试

2. **日期格式**：使用 `--date` 时必须使用 `YYYY-MM-DD` 格式（如 `2026-01-15`）

3. **公司名称**：使用 `--companies` 时，公司名称必须与数据库中的完全一致（区分大小写）

4. **数据去重**：程序会自动去重，相同日期的数据会被覆盖（使用 `INSERT OR REPLACE`）

5. **YouTube Shorts**：YouTube Shorts 会根据历史数据自动去重，避免重复爬取已存在的视频

## 🐛 常见问题

### Q1: 提示 "未找到任何公司"

**解决方案**：
- 检查数据库文件路径是否正确
- 确认数据库中是否有公司配置
- 使用 `--db-path` 指定正确的数据库路径

### Q2: 提示 "未配置 RAPIDAPI_KEY"

**解决方案**：
- 检查 `.env` 文件中是否配置了 `RAPIDAPI_KEY`
- 确认 API key 格式正确（没有多余的空格或特殊字符）

### Q3: 遇到 429 错误（API 限流）

**解决方案**：
- 配置多个 API key：`RAPIDAPI_KEY=key1;key2;key3`
- 等待一段时间后重试
- 检查 RapidAPI 套餐配额

### Q4: 爬取的数据为空

**可能原因**：
- 指定日期没有发布新内容
- API 返回的数据格式变化
- 平台配置不正确（username、user_id 等）

**解决方案**：
- 检查数据库中的平台配置是否正确
- 尝试爬取其他日期的数据
- 查看控制台输出的调试信息

## 📝 最佳实践

1. **日常爬取**：建议每天运行一次，爬取昨天的数据
   ```powershell
   python CompetitorDailyScraperFromDB.py --days-ago 1
   ```

2. **指定公司爬取**：如果需要重点关注某些公司，可以单独爬取
   ```powershell
   python CompetitorDailyScraperFromDB.py --companies "dream games" "vita studio" --days-ago 1
   ```

3. **定期备份**：建议定期备份数据库文件，以防数据丢失

4. **监控日志**：关注控制台输出的错误信息，及时处理异常

## 📚 相关文档

- [竞品时间段监控工作流使用说明.md](./竞品时间段监控工作流使用说明.md) - 时间段监控和分析工作流
- [数据库使用说明.md](./数据库使用说明.md) - 数据库结构和操作说明

