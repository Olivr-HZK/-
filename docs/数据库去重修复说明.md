# 数据库去重修复说明

## 🔧 修复内容

### 问题描述
之前的数据库保存逻辑存在去重问题：
- 当公司名、游戏名、平台类型一致时，没有正确覆盖原有配置
- 导致重复插入多条配置记录
- `game_name` 为 NULL 时，去重逻辑不正确

### 修复方案

**修改文件**: `CompetitorDatabaseDB.py`

**修改方法**: `_save_platform_record()`

**新的去重规则**:
- ✅ 基于 **公司名 + 游戏名（NULL 视为一致）+ 平台类型** 进行去重
- ✅ URL **不作为去重条件**（如果同一个平台有多个 URL，会更新为最新的 URL）
- ✅ 如果记录已存在，执行 **UPDATE** 更新所有字段
- ✅ 如果记录不存在，执行 **INSERT** 插入新记录

**关键改进**:
1. 使用显式的 `SELECT` 查询检查记录是否存在
2. 使用 `UPDATE` 语句更新已存在的记录（而不是 `INSERT OR REPLACE`）
3. 正确处理 `game_name` 为 NULL 的情况（使用 `IS NULL` 匹配）

---

## 📝 去重逻辑详解

### 去重条件

| 字段 | 是否参与去重 | 说明 |
|------|-------------|------|
| `company_name` | ✅ | 公司名称 |
| `game_name` | ✅ | 游戏名称（NULL 视为一致） |
| `platform_type` | ✅ | 平台类型（twitter, tiktok, instagram, youtube, facebook） |
| `url` | ❌ | URL 不作为去重条件，会更新为最新值 |

### 示例

**场景 1**: 公司级平台（game_name 为 NULL）
```python
# 第一次保存
company = "voodoo"
game_name = None
platform_type = "twitter"
url = "https://x.com/voodoo1"

# 第二次保存（URL 不同，但公司名、游戏名、平台类型相同）
company = "voodoo"
game_name = None
platform_type = "twitter"
url = "https://x.com/voodoo2"  # URL 更新

# 结果：更新现有记录，URL 变为 "https://x.com/voodoo2"
```

**场景 2**: 游戏级平台
```python
# 第一次保存
company = "voodoo"
game_name = "papers.io"
platform_type = "instagram"
url = "https://www.instagram.com/paper.io2game/"

# 第二次保存（相同配置）
company = "voodoo"
game_name = "papers.io"
platform_type = "instagram"
url = "https://www.instagram.com/paper.io2game/"

# 结果：更新现有记录（所有字段更新为最新值）
```

**场景 3**: 不同游戏（会创建新记录）
```python
# 第一次保存
company = "voodoo"
game_name = "papers.io"
platform_type = "instagram"

# 第二次保存（游戏名不同）
company = "voodoo"
game_name = "mob control"  # 不同的游戏
platform_type = "instagram"

# 结果：创建新记录（因为游戏名不同）
```

---

## 🛠️ 清空并重新加载配置脚本

### 脚本文件
`reload_companies_from_json.py`

### 功能
1. **清空数据库**：删除所有公司配置、平台配置、原始数据表、周报数据
2. **重新加载配置**：从 JSON 文件重新加载所有公司配置到数据库

### 使用方法

#### 基本用法（清空并重新加载）
```bash
python reload_companies_from_json.py --confirm
```

#### 指定 JSON 文件路径
```bash
python reload_companies_from_json.py --json-path input/my_config.json --confirm
```

#### 仅清空数据库（不加载配置）
```bash
python reload_companies_from_json.py --skip-load --confirm
```

#### 仅加载配置（不清空数据库）
```bash
python reload_companies_from_json.py --skip-clear --confirm
```

#### 交互式确认（不使用 --confirm）
```bash
python reload_companies_from_json.py
# 会提示输入 "yes" 确认
```

### 命令行参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--json-path` | JSON 配置文件路径 | `input/twitter_input.json` |
| `--db-path` | 数据库文件路径 | `db/competitor_data.db` |
| `--skip-clear` | 跳过清空数据库步骤 | False |
| `--skip-load` | 跳过加载配置步骤 | False |
| `--confirm` | 确认执行（避免误操作） | False |

### 清空的内容

脚本会清空以下数据：
1. ✅ `company_platforms` 表（所有平台配置）
2. ✅ `companies` 表（所有公司记录）
3. ✅ 所有 `{company}_raw_data` 表（原始爬取数据）
4. ✅ `company_tables_index` 表（公司表索引）
5. ✅ `weekly_reports` 表（周报数据）

**注意**: 清空操作不可逆，请谨慎使用！

---

## 🔄 使用流程

### 场景 1: 修复重复配置问题

```bash
# 1. 清空数据库并重新加载配置
python reload_companies_from_json.py --confirm

# 2. 验证配置是否正确
# （可以查看数据库或运行爬虫测试）

# 3. 正常使用爬虫
python CompetitorDailyScraperFromDB.py --days-ago 1
```

### 场景 2: 更新 JSON 配置后重新加载

```bash
# 1. 编辑 input/twitter_input.json，更新配置

# 2. 重新加载配置（不清空原始数据）
python reload_companies_from_json.py --skip-clear --confirm

# 或者直接运行爬虫（会自动从 JSON 同步配置）
python CompetitorDailyScraperFromDB.py --days-ago 1
```

### 场景 3: 仅清空配置（保留原始数据）

```bash
# 清空配置但保留历史爬取数据
python reload_companies_from_json.py --skip-load --confirm

# 然后重新加载配置
python reload_companies_from_json.py --skip-clear --confirm
```

---

## ⚠️ 注意事项

1. **备份数据**: 在执行清空操作前，建议备份数据库文件
   ```bash
   cp db/competitor_data.db db/competitor_data.db.backup
   ```

2. **确认 JSON 文件**: 确保 `input/twitter_input.json` 文件是最新的、正确的配置

3. **测试环境**: 建议先在测试环境验证，确认无误后再在生产环境执行

4. **去重逻辑**: 新的去重逻辑基于公司名+游戏名+平台类型，URL 不作为去重条件。如果同一个平台有多个 URL，只会保留最后一个。

5. **NULL 值处理**: `game_name` 为 NULL 时，使用 `IS NULL` 进行匹配，确保公司级平台能正确去重。

---

## 📊 验证修复效果

### 检查重复配置

```sql
-- 查看是否有重复配置（基于公司名+游戏名+平台类型）
SELECT 
    company_name,
    game_name,
    platform_type,
    COUNT(*) as count
FROM company_platforms
GROUP BY company_name, game_name, platform_type
HAVING COUNT(*) > 1;
```

如果查询结果为空，说明没有重复配置。

### 检查配置数量

```sql
-- 查看每个公司的平台配置数量
SELECT 
    company_name,
    COUNT(*) as platform_count
FROM company_platforms
GROUP BY company_name
ORDER BY platform_count DESC;
```

---

## 🔍 故障排查

### 问题 1: 仍然有重复配置

**可能原因**:
- JSON 文件中同一个平台配置了多次
- 公司名、游戏名、平台类型有细微差异（空格、大小写等）

**解决方法**:
1. 检查 JSON 文件，确保没有重复配置
2. 运行清空并重新加载脚本
3. 检查数据库中的实际数据

### 问题 2: 配置丢失

**可能原因**:
- JSON 文件中没有该配置
- 清空操作误删了配置

**解决方法**:
1. 检查 JSON 文件是否包含所有需要的配置
2. 从备份恢复数据库（如果已备份）

### 问题 3: URL 被覆盖

**可能原因**:
- 同一个公司、游戏、平台类型有多个 URL，但去重逻辑只保留一个

**解决方法**:
- 这是预期行为。如果需要保留多个 URL，需要修改去重逻辑，将 URL 也作为去重条件之一。

---

## 📝 总结

1. ✅ 修复了去重逻辑，现在基于公司名+游戏名+平台类型正确去重
2. ✅ 正确处理 `game_name` 为 NULL 的情况
3. ✅ 提供了清空并重新加载配置的脚本
4. ✅ 支持灵活的配置管理（清空、加载、组合操作）

现在可以安全地使用新的去重逻辑，避免重复配置问题。

