# 竞品监控数据库使用说明

## 概述

系统现在支持两种数据存储方式：
1. **JSON 文件模式**（默认）：数据存储在 `db/raw_data/` 目录下的 JSON 文件中
2. **SQLite 数据库模式**：数据存储在 SQLite 数据库中，每个公司一张表

## 启用数据库模式

### 方法1：环境变量（推荐）

设置环境变量 `COMPETITOR_USE_DATABASE=true`：

```bash
# Windows PowerShell
$env:COMPETITOR_USE_DATABASE="true"

# Windows CMD
set COMPETITOR_USE_DATABASE=true

# Linux/Mac
export COMPETITOR_USE_DATABASE=true
```

### 方法2：代码中指定

在创建 `CompetitorHistoryDB` 实例时指定：

```python
from CompetitorHistoryDB import CompetitorHistoryDB

# 使用数据库模式
db = CompetitorHistoryDB(use_database=True)

# 使用 JSON 模式（默认）
db = CompetitorHistoryDB(use_database=False)
```

## 数据库位置

- 默认路径：`db/competitor_data.db`
- 可通过环境变量 `COMPETITOR_DB_DIR` 指定目录
- 可通过 `CompetitorDatabaseDB(db_path="自定义路径.db")` 指定完整路径

## 数据迁移

### 将现有 JSON 数据迁移到数据库

使用迁移脚本 `migrate_json_to_database.py`：

```bash
# 迁移所有 JSON 文件
python migrate_json_to_database.py

# 试运行（不实际写入数据库）
python migrate_json_to_database.py --dry-run

# 只迁移指定日期的数据
python migrate_json_to_database.py --date 2026-01-13 2026-01-14

# 指定数据库目录
python migrate_json_to_database.py --db-dir db
```

### 迁移脚本功能

- 自动查找 `db/raw_data/` 目录下的所有 JSON 文件
- 支持两种文件结构：
  - 直接放在 `raw_data/` 下的文件（`YYYY-MM-DD.json`）
  - 按日期分文件夹的文件（`YYYY-MM-DD/YYYY-MM-DD.json`）
- 自动跳过已存在的数据（避免重复）
- 提供详细的迁移统计信息

## 数据库结构

### 表结构

每个公司一张表，表名格式：`company_raw_data_{公司名}`

```sql
CREATE TABLE company_raw_data_{company} (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    fetch_date TEXT NOT NULL,           -- 抓取日期 (YYYY-MM-DD)
    platform_type TEXT NOT NULL,       -- 平台类型 (twitter, tiktok, youtube等)
    game TEXT,                          -- 游戏名称（可选）
    url TEXT,                           -- 账号URL
    username TEXT,                      -- 用户名
    page_id TEXT,                       -- Facebook page_id
    channel_id TEXT,                    -- YouTube channel_id
    handle TEXT,                        -- YouTube handle
    posts_count INTEGER DEFAULT 0,      -- 帖子数量
    posts_json TEXT NOT NULL,           -- 帖子数据（JSON格式）
    fetched_at TEXT NOT NULL,           -- 抓取时间
    created_at TEXT NOT NULL,           -- 创建时间
    UNIQUE(fetch_date, platform_type, game, url)  -- 唯一约束
)
```

### 索引表

- `companies`：记录所有公司
- `company_tables_index`：公司名到表名的映射

## API 兼容性

`CompetitorHistoryDB` 类提供了统一的接口，无论使用 JSON 还是数据库模式，API 调用方式完全相同：

```python
from CompetitorHistoryDB import CompetitorHistoryDB

db = CompetitorHistoryDB()  # 自动根据环境变量选择模式

# 保存数据
db.save_raw_data(company, platforms_data, fetch_date)

# 加载数据
data = db.load_raw_data(company, fetch_date)
all_data = db.load_raw_data_by_date(fetch_date)

# 查询
companies = db.get_companies_for_date(fetch_date)
dates = db.get_all_dates_for_company(company)

# 获取视频ID（用于去重）
video_ids = db.get_platform_video_ids(
    company, game, platform_type, url, fetch_date
)
```

## YouTube Shorts 去重

`load_historical_youtube_shorts` 函数已更新，支持从数据库读取历史数据：

```python
from CompetitorScraperRapidAPI import load_historical_youtube_shorts

# 自动从数据库或 JSON 读取（取决于 CompetitorHistoryDB 的模式）
video_ids = load_historical_youtube_shorts(
    company="voodoo",
    game="mob control",
    platform_type="youtube",
    url="https://www.youtube.com/@MobControlAds/shorts",
    days_ago=1  # 查看1天前的数据
)
```

## 优势对比

### JSON 文件模式
- ✅ 简单直观，易于查看和编辑
- ✅ 无需额外配置
- ❌ 查询性能较低
- ❌ 数据量大时文件管理困难

### SQLite 数据库模式
- ✅ 查询性能高（索引优化）
- ✅ 数据完整性好（唯一约束）
- ✅ 易于扩展（可添加更多字段和索引）
- ✅ 结构化存储，便于管理
- ❌ 需要迁移现有数据

## 注意事项

1. **数据一致性**：迁移后，新数据会写入数据库，但旧的 JSON 文件不会自动删除
2. **向后兼容**：系统支持同时存在 JSON 和数据库数据，但建议统一使用一种模式
3. **备份**：定期备份数据库文件 `db/competitor_data.db`
4. **性能**：数据库模式在处理大量数据时性能更好

## 故障排查

### 问题：无法导入 CompetitorDatabaseDB

**解决方案**：确保 `CompetitorDatabaseDB.py` 文件存在，系统会自动回退到 JSON 模式

### 问题：迁移时提示数据已存在

**原因**：数据库中已有该日期的数据

**解决方案**：
- 检查数据库内容：`sqlite3 db/competitor_data.db "SELECT * FROM companies;"`
- 如需重新迁移，先删除数据库或相关表

### 问题：环境变量不生效

**解决方案**：
- 确认环境变量名称正确：`COMPETITOR_USE_DATABASE`
- 确认值设置为 `true`、`1` 或 `yes`（不区分大小写）
- 重启 Python 进程

